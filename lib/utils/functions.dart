// Color blendWithBlack(Color color, double opacity) {
//   return Color.alphaBlend(color.withValues(alpha: opacity), Colors.black);
// }
//
// Color lightenColor(Color color, double lightness) {
//   final hsl = HSLColor.fromColor(color);
//   return hsl.withLightness(lightness.clamp(0.0, 1.0)).toColor();
// }

import 'package:flutter/material.dart';

class AzkarSingle extends StatelessWidget {
  const AzkarSingle({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Card View Content (بطاقات)'));
  }
}
